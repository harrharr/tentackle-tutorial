<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example</groupId>
    <artifactId>tracker</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>tracker-pdo</artifactId>
  <packaging>jar</packaging>
  <name>Tracker PDO</name>
  <description>PDO Layer for Tracker</description>

  <properties>
    <rootBasedir>${project.parent.basedir}</rootBasedir>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.example</groupId>
      <artifactId>tracker-common</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.wurbelizer</groupId>
        <artifactId>wurbelizer-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.tentackle</groupId>
        <artifactId>tentackle-sql-maven-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <goals>
              <goal>create</goal>
            </goals>
            <configuration>
              <verbosity>info</verbosity>
              <dumpDir>${project.build.directory}/generated-resources/META-INF/model</dumpDir>
              <dumpModelSource>true</dumpModelSource>
              <dumpModelIndex>${project.build.directory}/generated-resources/META-INF/MODEL-INDEX.LIST</dumpModelIndex>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <!-- migrate the tentackle tables -->
      <id>migrate-tentackle</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.tentackle</groupId>
            <artifactId>tentackle-sql-maven-plugin</artifactId>
            <configuration>
              <loadModelFromClassPath>true</loadModelFromClassPath>
              <checkReservedTables>false</checkReservedTables>
            </configuration>
            <dependencies>
              <dependency>
                <groupId>org.tentackle</groupId>
                <artifactId>tentackle-test-pdo</artifactId>
                <version>${tentackle.version}</version>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!-- extract the model to target/wurbel/model without code generation -->
      <id>wurbel-model-only</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.wurbelizer</groupId>
            <artifactId>wurbelizer-maven-plugin</artifactId>
            <configuration>
              <lastPhase>0</lastPhase>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
